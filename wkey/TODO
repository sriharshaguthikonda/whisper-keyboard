# Runtime TODO

- [x] Resolve default input device before creating `sounddevice.InputStream`.
- [x] Preserve full pre-recording ring-buffer chunks across wraparound.
- [x] Confirm `There_is_a_device` is not active in tracked code.
- [ ] Extend hardware detection beyond microphone-present checks if device selection still fails after hibernate.
- [ ] Revisit old audio-subtract branch before restoring any audio-subtraction behavior.
